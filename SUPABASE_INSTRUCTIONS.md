# دليل ربط واستضافة منصة حجز الأستاذ محمود الديب مع Supabase و Vercel

مرحباً بك! هذا الدليل مخصص لمساعدتك خطوة بخطوة لربط هذا الموقع بقاعدة بيانات **Supabase** الخاصة بك ورفع الموقع على **Vercel** ليعمل بشكل حي وسريع جداً.

---

## الجزء الأول: إعداد قاعدة البيانات في Supabase

1. **إنشاء مشروع جديد:**
   - اذهب إلى موقع [Supabase](https://supabase.com) وسجل الدخول.
   - اضغط على **New Project** (مشروع جديد).
   - اختر اسماً للمشروع (مثال: `eldeeb-booking`) وحدد كلمة مرور قوية لقاعدة البيانات، واختر المنطقة الأقرب لك (مثال: `Europe (Frankfurt)`).

2. **تنفيذ الأوامر البرمجية (SQL):**
   - بعد فتح لوحة التحكم الخاصة بالمشروع الجديد، اذهب إلى القائمة الجانبية واختر **SQL Editor** (أيقونة `>_`).
   - اضغط على **New Query** لفتح نافذة فارغة.
   - افتح ملف `supabase_schema.sql` المرفق مع هذا الموقع، وانسخ كل محتواه.
   - الصق الكود داخل محرّر SQL في Supabase واضغط على زر **Run** في الأسفل.
   - ستظهر لك رسالة نجاح `Success` مما يعني أنه تم إنشاء الجداول (`bookings` و `groups`) وإضافة المجموعات الافتراضية بنجاح.

3. **الحصول على مفاتيح الربط (API Keys):**
   - اذهب إلى **Project Settings** (أيقونة الترس في القائمة الجانبية).
   - اختر قسم **API**.
   - ستجد حقلين هامين جداً:
     - **Project URL** (رابط المشروع)
     - **anon public key** (المفتاح العام)
   - احتفظ بهذين الرابطين لتضعهما في إعدادات البيئة (موضحة في الخطوة التالية).

---

## الجزء الثاني: ربط مفاتيح Supabase بالموقع

في بيئة التطوير والرفع، نستخدم متغيرات البيئة (Environment Variables) للحفاظ على سرية البيانات وأمنها.

### 1. في جهازك الشخصي للتجربة المحلية:
قم بإنشاء ملف باسم `.env` أو عدّل الملف المرفق `.env.example` واحفظه باسم `.env` في المجلد الرئيسي للمشروع، وضع فيه الرموز كالتالي:

```env
VITE_SUPABASE_URL="ضع_رابط_المشروع_هنا"
VITE_SUPABASE_ANON_KEY="ضع_المفتاح_العام_هنا"
```

### 2. عند الرفع على Vercel أو أي منصة استضافة أخرى:
أثناء خطوات الرفع (كما سنشرح لاحقاً)، سيطلب منك موقع الاستضافة إضافة **Environment Variables**. قم بإضافة الحقلين التاليين وقيمتهما التي نسختها من Supabase:
- اسم المتغير الأول: `VITE_SUPABASE_URL`
- اسم المتغير الثاني: `VITE_SUPABASE_ANON_KEY`

---

## الجزء الثالث: طريقة الرفع على منصة Vercel (مجاناً بالكامل)

منصة **Vercel** هي الأفضل لرفع هذا النوع من المواقع لتوفر سرعة استجابة فائقة للطلاب وضمان استقرار تام:

1. **رفع الكود على GitHub:**
   - قم بإنشاء مستودع (Repository) جديد على حسابك في GitHub (مثال باسم `eldeeb-booking`).
   - ارفع كود هذا المشروع بالكامل إلى المستودع.

2. **الربط مع Vercel:**
   - اذهب إلى موقع [Vercel](https://vercel.com) وسجل دخولك باستخدام حساب GitHub الخاص بك.
   - اضغط على زر **Add New...** ثم اختر **Project**.
   - ستظهر لك قائمة بمستودعات GitHub الخاصة بك. اختر مستودع `eldeeb-booking` واضغط على **Import**.

3. **تكوين الإعدادات وإضافة متغيرات البيئة:**
   - اترك جميع إعدادات البناء (Build Settings) كما هي افتراضية (سيتعرف Vercel على أنه مشروع Vite تلقائياً).
   - قبل الضغط على Deploy، ابحث عن قسم **Environment Variables** (متغيرات البيئة) وقم بإضافة المفاتيح التي نسختها من Supabase:
     - أضف `VITE_SUPABASE_URL` كاسم، والـ URL الخاص بـ Supabase كقيمة.
     - أضف `VITE_SUPABASE_ANON_KEY` كاسم، والمفتاح العام لـ Supabase كقيمة.
   - اضغط على زر **Deploy**.

4. **الموقع أصبح حياً!**
   - خلال دقيقة واحدة، سينتهي Vercel من بناء الموقع وسيعطيك رابطاً حياً ومباشراً (مثال: `https://eldeeb-booking.vercel.app`).
   - يمكنك مشاركة هذا الرابط مباشرة مع الطلاب لتبدأ عمليات الحجز.

---

## الجزء الرابع: مميزات النظام وتسهيل الاستخدام

* **رابط لوحة التحكم السرية:**
  يمكنك الوصول إلى لوحة تحكم الإدارة لمشاهدة الطلاب المسجلين، إحصائياتهم، تصدير البيانات إلى Excel، وإدارة المجموعات عبر الذهاب إلى الرابط السري التالي مباشرة (استبدل رابط الموقع برابط موقعك الفعلي):
  `https://your-website.vercel.app/a/a/a/a/2/0/0/2/3`
  *(ملاحظة: هذا الرابط مخفي تماماً ولا يوجد له أي زر في الواجهة الرئيسية لضمان عدم دخول الطلاب).*

* **المجموعات التلقائية الذكية:**
  الطلاب لن يختاروا مجموعة أثناء التسجيل. كمسؤول، يمكنك إضافة وتعديل المجموعات من لوحة التحكم السرية وربط كل مجموعة بمسار دراسي معين (طب، هندسة، أعمال، آداب وفنون). بمجرد تسجيل الطالب وتحديد مساره، سيقوم النظام تلقائياً بتخصيصه للمجموعة المناسبة له وطباعتها فوراً على الإيصال الإلكتروني الخاص به!

بالتوفيق للأستاذ محمود الديب وجميع الطلاب الكرام للعام الدراسي 2026 - 2027! 🌟
